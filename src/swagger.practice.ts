/*swagger */

// const options = new DocumentBuilder()
//   .setTitle("Userapp")
//   .setDescription("user rest api docs")
//   .setVersion("1.0")
//   .build();


// const options: This declares a constant variable named options, which will be used to hold the Swagger document configuration.

// new DocumentBuilder(): This creates a new instance of the DocumentBuilder class from the @nestjs/swagger package. The DocumentBuilder is a utility class that allows you to build the configuration for your Swagger documentation.

// .setTitle("Userapp"): This sets the title of your API documentation to "Userapp". It will be displayed at the top of the Swagger UI page.

// .setDescription("user rest api docs"): This sets the description of your API documentation to "user rest api docs". The description provides additional information about your API and is displayed on the Swagger UI page.

// .setVersion("1.0"): This sets the version of your API to "1.0". The version number is typically used to indicate the version of your API and can be helpful when dealing with multiple API versions.

// const document = SwaggerModule.createDocument(app, options);

// const document: This declares a constant variable named document, which will hold the Swagger JSON document generated based on the provided options and your application's routes.

// SwaggerModule.setup("api", app, document);







